
import React, {useState} from 'react';
import {
  TouchableOpacity,
  TextInput,
  View,
  Text,
  ImageBackground,
  SafeAreaView,
  StyleSheet,
  Image,
  Dimensions,
} from 'react-native';
import Images from '../../config/im';
import {calculateFontSize} from '../../config/font';
const {width, height} = Dimensions.get('window');
export const POstUPloderprofile = () => {
  return (
    <SafeAreaView>
        <View>
            <Text>profile</Text>
        </View>
    </SafeAreaView>
  )
}
const style= StyleSheet.create({
      conatiner:{


 }
})